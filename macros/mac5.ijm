// Example - Conditional branching

area = 1;
intensity = 10;
if ((area < 2) && (intensity >= 5)) {
	print("This is small and bright.");
} else if (area < 2) {
	print("This is just small.");
} else {
	print("This is big.");
}