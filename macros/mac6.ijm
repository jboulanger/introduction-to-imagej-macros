// Example - Loops

N = 3; // Number of loops
// For loop starting at 1
for (i = 1; i <= N; i++) {
	print(i);
}

// For loop starting at 0
for (i = 0; i < N; i++) {
	print(i);
}

// While loop
i = 0; // initialization
while (i < N) {
	print(i);
	i = i + 1; // try to comment out this line :-/
}